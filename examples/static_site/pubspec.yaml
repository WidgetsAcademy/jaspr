name: static_site
description: A pure client-side app without ssr.
version: 0.0.1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  jaspr: ^0.9.0

dev_dependencies:
  build_runner: ^2.4.0
  jaspr_web_compilers: ^4.0.4
  lints: ^2.1.0

jaspr:
  uses-ssr: false
