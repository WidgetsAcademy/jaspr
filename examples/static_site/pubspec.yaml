name: static_site
description: A pure client-side app without ssr.
version: 0.0.1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  jaspr: '>=0.9.0 <2.0.0'

dev_dependencies:
  build_runner: ^2.0.0
  jaspr_web_compilers: ^4.0.0
  lints: ^2.0.0

jaspr:
  uses-ssr: false
